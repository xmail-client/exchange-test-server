{
  "name": "example",
  "version": "1.0.0",
  "description": "just the example for exchange-test-server package",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "liuxiong332",
  "license": "ISC",
  "dependencies": {
    "exchange-test-server": "^0.1.1",
    "libxmljs-builder": "^0.3.0"
  }
}
